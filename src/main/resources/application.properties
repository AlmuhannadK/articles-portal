#############################################################
# setup local h2 database config (temp)
spring.datasource.url=jdbc:h2:file:./data/articlesapp;NON_KEYWORDS=user
spring.datasource.driver-class-name=org.h2.Driver
spring.datasource.username=admin
spring.datasource.password=admin
spring.jpa.database-platform=org.hibernate.dialect.H2Dialect

spring.h2.console.enabled=true
spring.h2.console.path=/h2-console

spring.jpa.hibernate.ddl-auto=create-drop

#Hibernate will create our tables before inserting the data into them via scripts
#spring.jpa.defer-datasource-initialization=true
#spring.sql.init.mode=always


#############################################################

# better to use MySql from now on
